# 專案規則：Markdown 存放位置

## 原則
- 所有新增或修改的 `.md` 檔案（含 README、說明文件、筆記或任務追蹤）**必須**放在 `docs/` 目錄下，避免散落於專案根目錄或其他資料夾。
- 若有特殊例外需求（例如第三方套件強制放特定路徑），需先與團隊確認並在此文件註明。

## 運用方式
- 建立 `.md` 檔案時，直接在 `docs/` 開頭新增，並保持命名一致性，必要時在 docs 內新增子目錄區隔領域。
- 若發現已有 `.md` 在其他地方，應主動移入 `docs/` 並更新相關引用或連結。
